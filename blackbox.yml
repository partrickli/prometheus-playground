modules:
  http_2xx:
    prober: http
    http:
      preferred_ip_protocol: 'ip4' # defaults to "ip6"
  http_post_2xx:
    prober: http
    http:
      method: POST
  icmp_ipv4:
    prober: icmp
    icmp:
      preferred_ip_protocol: ip4
